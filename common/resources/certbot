# /etc/cron.d/certbot: crontab entries for the certbot package
#
# Upstream recommends attempting renewal twice a day
#
# Renewal will only occur if expiration is within 30 days.
SHELL=/bin/sh
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# Run renew every 12h, but only actually renew on the MASTER node.
0 */12 * * * test -x /usr/local/bin/certbot-renew && sleep $(expr $RANDOM % 3600)s && /usr/local/bin/certbot-renew -q && haproxy-refresh
