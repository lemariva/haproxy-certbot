# Non-interactive defaults
agree-tos = true
non-interactive = true

# Key & cert defaults
rsa-key-size = 4096
preferred-chain = ISRG Root X1

# Logging
# (actual paths are controlled by --config-dir/--logs-dir from wrapper)
text = true
verbose = 0

# Challenge preference (still compatible with webroot)
preferred-challenges = http